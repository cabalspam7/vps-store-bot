# Kosong dan memang sengaja.
#
# Bot ini hanya memakai Python standard library (urllib, sqlite3, threading,
# http.server), jadi tidak ada yang perlu di-install. Cukup Python 3.8+.
#
#   python3 bot.py
